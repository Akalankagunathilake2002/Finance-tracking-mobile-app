<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Declare the POST_NOTIFICATIONS permission -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:maxSdkVersion="33" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Labexam3"
        tools:targetApi="31">
        <activity
            android:name=".Loading2"
            android:exported="false" />
        <activity
            android:name=".Loading1"
            android:exported="false" />
        <activity
            android:name=".RestoreDataActivity"
            android:exported="false" />
        <activity
            android:name=".SetBudgetActivity"
            android:exported="false" /> <!-- Loading Activity as the entry point -->
        <activity
            android:name=".Loading"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- MAIN action -->
                <category android:name="android.intent.category.LAUNCHER" /> <!-- LAUNCHER category -->
            </intent-filter>
        </activity> <!-- Other Activities -->
        <activity
            android:name=".AddTransactionActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>